"""Benchmark and tuning tooling — not part of the attention library proper.

- ``benchmark`` — main attention benchmark driver.
- ``benchmark_flash_attention_fp8`` — fp8 benchmark.
- ``bench_utils`` — shared timing/reporting helpers.
- ``sm90_config_search`` — SM90 tile-config autotuning sweep.
"""
